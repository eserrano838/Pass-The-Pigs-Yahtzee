package passthepigs;

public class PassThePigs {
    public static void main(String[] args) {
        GameScreen game = new GameScreen();
        game.playGame();
        /*
        Hand hand1 = new Hand();
        ScoreBoard score1 = new ScoreBoard(hand1);
        
        Hand hand2 = new Hand();
        ScoreBoard score2 = new ScoreBoard(hand2);   
        
        score1.setFinalScore(0,4);
        score1.setFinalScore(1,6);
        score1.setFinalScore(2,5);
        score1.setFinalScore(3,7);
        score1.setFinalScore(4,10);
        score1.setFinalScore(5,20);
        score1.setFinalScore(6,25);
        score1.setFinalScore(7,30);
        score1.setFinalScore(8,35);
        score1.setFinalScore(9,0);
        score1.setFinalScore(10,10);
        score1.setFinalScore(11,0);
        
        score2.setFinalScore(0,4);
        score2.setFinalScore(1,3);
        score2.setFinalScore(2,5);
        score2.setFinalScore(3,7);
        score2.setFinalScore(4,0);
        score2.setFinalScore(5,0);
        score2.setFinalScore(6,25);
        score2.setFinalScore(7,30);
        score2.setFinalScore(8,35);
        score2.setFinalScore(9,40);
        score2.setFinalScore(10,10);
        score2.setFinalScore(11,0);
        
        score1.setBonus();
        score1.setTotal();
        game.displayFinalScore(score1);
        score2.setBonus();
        score2.setTotal();
        game.displayFinalScore(score2);
        */
    }
}
